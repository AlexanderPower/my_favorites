- model_class = Company
.page-header
  %h1=t('.title', :default => model_class.model_name.human.titleize)+ " #{@company.name}"

%h3 People in company:
%table.table.table-striped
  %thead
    %tr
      %th Name
  %tbody
    - @company.people.each do |person|
      %tr
        %td= person.name